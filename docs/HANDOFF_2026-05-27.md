# HANDOFF — OmegaWiki, sesión 2026-05-26 → continuación 2026-05-27

Documento para arrancar una conversación nueva con todo el contexto.

## Estado del wiki al cerrar

- papers: ~36 · claims: ~674 · concepts: ~248 · edges: ~1563
- Lint: 0🔴 0🟡 (al cerrar, tras arreglar el ingest de Novae)
- Rama: main, todo commiteado y pusheado a PepCalafell/OmegaWiki
- Último trabajo: ingest de Novae (graph-based foundation model spatial)

## Flujo de trabajo establecido

1. `.venv/bin/python scripts/next_paper.py` — qué paper toca
2. `/ingest "..."` en Claude Code (Opus, medium effort — único modelo viable)
3. `./scripts/verify_paper.sh` — validador + lint + formato. ESPERAR a 0🔴.
4. `./scripts/commit_paper.sh` — pide TÍTULO (una línea, a mano,
   formato `ingest: Autor AÑO Venue — tema`) y luego CUERPO (pegar el
   resumen del /ingest). Re-verifica y aborta si hay 🔴.

REGLA DE ORO: nunca commitear con el verify en rojo. Verify limpio → commit.

## Catálogo de bugs y cómo se arreglan

### Bug 1 — formato source_papers / key_papers (CERRADO)
- Síntoma: claims/concepts con `source_papers: [slug]` inline en vez de
  lista YAML multilínea.
- Causa raíz: el template `runtime-page-templates` mostraba `[]` inline;
  Opus copia el template, no la tabla de reglas.
- Fix aplicado: template corregido a formato multilínea + comentario
  explícito. Verificado en varios ingests. CERRADO.

### Bug 2 — Mejora 7: enums inválidos (INTERMITENTE, mitigado)
- Síntoma: valores de enum que no existen. Vistos hoy:
  - concepts `maturity`: `mainstream`, `established` → correcto: `stable`
  - foundations `status`: `emerging` → correcto: `mainstream`
  - claims `status`: `hypothesis` → correcto: `proposed`
- Valores válidos:
  - concepts.maturity: stable | active | emerging | deprecated
  - foundations.status: mainstream | historical
  - claims.status: proposed | weakly_supported | supported | challenged | deprecated
- Causa: intermitencia del modelo. NO es el template (está correcto).
  No depende del paper; un paper grande solo amplifica la frecuencia.
- Fix: corregir el valor a mano (script Python de sustitución).
  Mitigación: comentarios imperativos en el template. NO lo elimina.
- Red de seguridad: verify_paper.sh lo caza antes del commit.

### Bug 3 — esquema de foundations inventado (Mejora 7, mitigado)
- Síntoma: foundations con campos `type: foundation` / `category: gene`
  y SIN los obligatorios `domain` / `status`.
- Esquema correcto de foundation: title, slug, domain, status, aliases,
  first_introduced, date_updated, source_url. NADA de type/category.
- Fix: añadir domain (ej. `immunology`) + status (`mainstream`),
  eliminar type/category.
- Documentado en INGEST_AUDIT.md sección 9.9.

### Bug 4 — campo tags faltante en concepts (Mejora 7)
- Síntoma: concepts sin el campo `tags` (obligatorio).
- Fix: añadir `tags: []` al frontmatter.

### Bug 5 — wikilink huérfano
- Síntoma: `[[wiki/projects/thesis]]` u otro wikilink a página inexistente.
- Fix: quitar los corchetes (era prosa, no un enlace real) o apuntar a
  una página que exista.

### Bug 6 — linter xref-asymmetry (PARCIALMENTE arreglado)
- El check de claims se arregló (prefijo + regex multilínea).
- PENDIENTE: los checks de concepts/people/ideas (lint.py L313/325/355)
  tienen el mismo bug sin parchear. Ver INGEST_AUDIT.md 9.5.

## Deuda pendiente (para revisar)

- Checks linter xref-asymmetry 313/325/355 sin parchear.
- Campo `evidence` de los claims nunca auditado para consistencia.
- POSIBLES ERRORES SEMÁNTICOS: verify_paper.sh caza errores
  ESTRUCTURALES (enums, campos), NO semánticos (claim mal atribuido,
  edge incorrecto, concepto duplicado con otro nombre). Se procesaron
  muchos papers seguidos; conviene una pasada de revisión de contenido
  de los papers recientes.
- Commits con títulos pobres en el log (varios "chore" duplicados, un
  "Zhou,"). Cosméticos, no se tocan — reescribir historia remota no compensa.

## Lecciones de mecánica (terminal)

- Pegar UN bloque cada vez, esperar a que termine antes del siguiente.
- Antes de pulsar ENTER, mirar que la última línea sea un comando, no
  texto pegado de más.
- Heredocs largos (`cat << EOF`) se cortan al pegar — para archivos
  largos usar el editor (nano) en vez de heredoc.

## Primeras acciones sugeridas para mañana

1. `git pull` y `git log --oneline -5` para confirmar el estado.
2. `./scripts/verify_paper.sh` sobre el último paper — confirmar 0🔴.
3. Decidir: continuar ingestando, o dedicar la sesión a revisar la
   deuda (errores semánticos, checks del linter).
