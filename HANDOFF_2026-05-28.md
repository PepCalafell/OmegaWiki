# HANDOFF — OmegaWiki, sesión 2026-05-27 → continuación 2026-05-28

Documento para arrancar una conversación nueva con todo el contexto.

## Estado del wiki al cerrar

- papers: 37 · claims: 697 · concepts: 252 · foundations: 347+ · edges: 1611
- Lint: 0🔴 0🟡 al cerrar
- Rama: main, todo commiteado y pusheado a PepCalafell/OmegaWiki
- Último commit: beeae56 — ingest Hamon 2025 (paper #70, TREM2/HCC)

## Lo que cambió esta sesión (IMPORTANTE — leer antes de tocar nada)

### 1. Nuevo check de duplicados
- `tools/check_duplicates.py` — detecta duplicados léxicos de conceptos
  (slug/alias parecidos) + duplicados intra-paper de claims.
- Integrado en `verify_paper.sh` como BLOQUE 4. Es 🟡, NO bloquea el commit.
- Umbral: equilibrado. NO usa IA, sin dependencias nuevas.
- Limitación conocida: solo caza duplicados LÉXICOS (nombres parecidos).
  NO ve el duplicado semántico con nombres distintos.
- Falso positivo conocido y esperado: el par de claims Novae breast/colon
  (ratio 0.95) — son dos benchmarks reales distintos. NO tocar, ignorarlo.

### 2. .claude/ es la FUENTE DE VERDAD — setup.sh neutralizado
- `.claude/skills/ingest/` contiene la adaptación biomédica. `i18n/en/`
  quedó congelado en el original genérico de upstream.
- `setup.sh` Step 3b (copiaba i18n/ -> .claude/) está COMENTADO.
- ⚠️ NUNCA correr `setup.sh` esperando que sincronice skills: machacaría
  la adaptación. El resto de setup.sh (venv/deps/.env) sigue siendo seguro.
- `.claude/skills/ingest/SKILL.md` lleva un comentario <!-- FUENTE DE VERDAD -->.

### 3. Sección B del SKILL.md de ingest reescrita — aliases estrictos
- Antes: "aliases anchos, 6-12, terminología que otros papers usarían" +
  HARD FAIL si <6. Causaba colisiones en find-similar-concept (cada alias
  se compara contra cada alias, basta uno ancho compartido para falso match).
- Ahora: aliases = solo sinónimos estrictos. Test de exclusividad. Sin
  mínimo. HARD FAIL eliminado. Cambiado en secciones B, E y Constraints.
- VALIDADO con el ingest del #70: 5 conceptos nuevos, 0 colisiones, merge
  limpio del trem2-tumor-associated-macrophage existente. FUNCIONA.

### 4. Fixes aplicados al wiki
- trem2-tam-hypoxic-niche-immune-suppression: era un concepto que duplicaba
  un claim ya existente. Eliminado, 6 referencias redirigidas.
- 2 colisiones de alias resueltas (quitado 'invasive front signature' y
  'zero-shot spatial transcriptomics' de un concepto cada una).

## Flujo de trabajo (sin cambios)

1. `.venv/bin/python scripts/next_paper.py` — qué paper toca
2. `/ingest "..."` en Claude Code (Opus, medium effort)
3. `./scripts/verify_paper.sh` — ESPERAR a 0🔴 (el bloque 4 🟡 no bloquea)
4. `./scripts/commit_paper.sh` — pide TÍTULO y CUERPO
   - Al pegar el cuerpo, RECORTAR las líneas de ruido de Claude Code
     (`✻ Crunched...`, `※ recap:...`) — no son parte del resumen.

REGLA DE ORO: nunca commitear con verify en 🔴.

## Deuda pendiente (ordenada por prioridad)

1. LIMPIEZA DE ALIASES VIEJOS. Los ~247 conceptos preexistentes conservan
   aliases anchos. La sección B arregla el grifo, no el charco. Pendiente
   una pasada de limpieza. → Candidato a la próxima sesión: acotado y cierra
   el tema de aliases del todo.

2. FORMATO DE CLAIMS [cnn] vs [slug]. El ingest del #70 generó los claims
   con etiqueta [slug] en vez de [cnn]; hubo que renumerar a mano. Decisión
   de diseño abierta: ¿migrar todo a [slug] (mejor, auto-documentado) o
   mantener [cnn]? Requiere tocar SKILL + validate_step4e.py + 37 papers.
   Sesión propia.

3. verify_paper.sh ELIGE EL PAPER POR FECHA DE MODIFICACIÓN. Frágil: un
   ingest que toca papers viejos (por edges) le cambia el mtime y el verify
   acaba validando el paper equivocado. Pasó con el #70. Considerar pasarle
   el slug del paper recién ingestado explícitamente.

4. AUDITORÍA SEMÁNTICA REAL. Sigue sin hacerse. Leer claims contra el PDF
   en ~5 papers y medir tasa de error de atribución. El check de duplicados
   NO cubre esto.

5. foundations: 347+ — más foundations que conceptos. La sección C del SKILL
   crea una foundation por cada método (Seurat, ChIP-seq...). Revisar si
   sobre-crea. Relacionado con Bugs 3/7 del handoff anterior.

6. Bug 2 (enums inválidos) SIGUE VIVO. El #70 produjo 'tentatively_supported'
   (corregido a 'weakly_supported'). Intermitente, lo caza el verify.

7. i18n/en/skills/ingest/ quedó obsoleto (no refleja la versión activa).
   Marcar como OBSOLETO o congelar para no confundir al yo futuro.

## Catálogo de bugs heredado (del handoff anterior, sigue vigente)

- Bug 2 — enums inválidos: intermitente, no es el template. Fix: corregir
  a mano al valor válido. Red de seguridad: verify_paper.sh lo caza.
  Válidos: concepts.maturity {stable|active|emerging|deprecated};
  claims.status {proposed|weakly_supported|supported|challenged|deprecated};
  foundations.status {mainstream|historical}.
- Bug 3 — foundations con esquema inventado (type/category en vez de
  domain/status). Corregir a mano.
- Bug 6 — linter xref-asymmetry: checks de concepts/people/ideas
  (lint.py L313/325/355) sin parchear. Pendiente.

## Primeras acciones sugeridas para mañana

1. `git pull` y `git log --oneline -5` — confirmar estado.
2. `./scripts/verify_paper.sh` — confirmar 0🔴.
3. Decidir: atacar deuda 1 (limpieza de aliases viejos, recomendado) o
   continuar ingestando con la skill nueva ya validada.
